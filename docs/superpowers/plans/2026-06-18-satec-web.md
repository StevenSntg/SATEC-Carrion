# Plan 4 — Aplicación web (mapa de riesgo) — SATEC

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:executing-plans. Steps use checkbox (`- [ ]`).

**Goal:** Una aplicación web nueva e independiente que muestre un **mapa de riesgo de brotes de Carrión por provincia** y un **comparador Red Neuronal vs Árbol de Decisión**, con diseño propio profesional, desplegable de forma estática.

**Architecture:** Dos fases. (A) **Datos** — en Python: entrenar RN y AD sobre todo el dataset, exportar la RN a TensorFlow.js y el árbol a JSON, y precomputar el riesgo por provincia para la última semana disponible. (B) **Interfaz** — página estática (HTML + React por CDN + Leaflet) que pinta el GeoJSON provincial coloreado por riesgo y, al hacer clic, muestra el detalle (predicción de RN y AD, canal endémico).

**Tech Stack:** Python 3.12 + tf_keras + tensorflowjs 4.22 (export); HTML/CSS + React 18 (CDN) + TensorFlow.js + Leaflet (web); Vercel (hosting estático). Node 24 / npm 11 disponibles.

## Global Constraints

- Solo **Red Neuronal** y **Árbol de Decisión** como modelos (sin ensembles).
- La web NO reentrena: carga modelos exportados. El árbol se recorre con JS plano; la RN corre con TF.js.
- Diseño **independiente** del proyecto previo (Titanic/Carrión), tema propio de salud pública/epidemiología.
- Solo se publica `web/public/`. Commits: cuenta **stevensntg**, sin coautoría de Claude.

---

### Task 1: Exportar modelos entrenados (RN → TF.js, AD → JSON)

**Files:**
- Create: `src/satec/web_export/__init__.py`, `src/satec/web_export/export_models.py`
- Create: `src/satec/web_export/export_tree.py`
- Test: `tests/web_export/__init__.py`, `tests/web_export/test_export_tree.py`

**Interfaces:**
- `tree_to_dict(clf, feature_names, class_labels) -> dict` (arreglos children_left/right, feature, threshold, value).
- `export_all(repo)` → entrena RN y AD sobre TODO el dataset enriquecido y escribe:
  `web/public/models/rn/model.json` (+ .bin), `web/public/models/ad.json`,
  `web/public/models/norm.json` (min/rng + feature_names).

- [ ] **Step 1: Test del exportador de árbol** (`tests/web_export/test_export_tree.py`)

```python
from sklearn.tree import DecisionTreeClassifier
import numpy as np
from satec.web_export.export_tree import tree_to_dict


def test_tree_to_dict_estructura():
    X = np.array([[0.0], [1.0], [0.0], [1.0]])
    y = np.array([0, 1, 0, 1])
    clf = DecisionTreeClassifier(max_depth=1).fit(X, y)
    d = tree_to_dict(clf, ["x"], ["no", "si"])
    assert "children_left" in d and "feature" in d and "threshold" in d
    assert d["feature_names"] == ["x"] and d["class_labels"] == ["no", "si"]
    assert len(d["children_left"]) == len(d["feature"])
```

- [ ] **Step 2: Ver fallo**
Run: `py -3.12 -m pytest "C:/Users/Usuario/Desktop/IA/SATEC-Carrion/tests/web_export/test_export_tree.py" -v`
Expected: FAIL `ModuleNotFoundError`

- [ ] **Step 3: Implementar `export_tree.py`**

```python
"""Serializa un DecisionTreeClassifier de sklearn a un dict JSON plano."""


def tree_to_dict(clf, feature_names, class_labels) -> dict:
    t = clf.tree_
    return {
        "feature_names": list(feature_names),
        "class_labels": list(class_labels),
        "children_left": t.children_left.tolist(),
        "children_right": t.children_right.tolist(),
        "feature": t.feature.tolist(),
        "threshold": t.threshold.tolist(),
        "value": t.value.tolist(),
    }
```

- [ ] **Step 4: Implementar `export_models.py`** (y `__init__.py`)

```python
"""Entrena RN y AD sobre todo el dataset y los exporta para la web."""
import json
import os
import shutil
import numpy as np
import pandas as pd

from satec.models.features_matrix import feature_matrix, FEATURE_COLS
from satec.models.train import train_decision_tree, train_neural_net
from satec.web_export.export_tree import tree_to_dict

CLASS_LABELS = ["no_brote", "brote"]


def export_all(repo: str = ".") -> None:
    df = pd.read_parquet(
        os.path.join(repo, "data/processed/dataset_enriched.parquet"))
    X, y = feature_matrix(df)

    out_dir = os.path.join(repo, "web", "public", "models")
    os.makedirs(out_dir, exist_ok=True)

    # Arbol de Decision (poda 8, el de mejor balance) -> JSON
    ad = train_decision_tree(X, y, max_depth=8)
    with open(os.path.join(out_dir, "ad.json"), "w", encoding="utf-8") as f:
        json.dump(tree_to_dict(ad, FEATURE_COLS, CLASS_LABELS), f)

    # Red Neuronal -> TensorFlow.js + norm.json
    model, norm = train_neural_net(X, y, epochs=60)
    with open(os.path.join(out_dir, "norm.json"), "w", encoding="utf-8") as f:
        json.dump({"feature_names": FEATURE_COLS,
                   "min": norm["min"].tolist(),
                   "rng": norm["rng"].tolist()}, f)
    rn_dir = os.path.join(out_dir, "rn")
    if os.path.exists(rn_dir):
        shutil.rmtree(rn_dir)
    os.makedirs(rn_dir, exist_ok=True)
    import tensorflowjs as tfjs
    tfjs.converters.save_keras_model(model, rn_dir)
    print(f"[OK] modelos exportados a {out_dir}")


if __name__ == "__main__":
    export_all()
```

- [ ] **Step 5: Tests + exportación real**
Run:
```bash
REPO="C:/Users/Usuario/Desktop/IA/SATEC-Carrion"
py -3.12 -m pytest "$REPO/tests/web_export" -v
PYTHONPATH="$REPO/src" TF_USE_LEGACY_KERAS=1 py -3.12 -c "from satec.web_export.export_models import export_all; export_all(r'$REPO')"
ls -R "$REPO/web/public/models"
```
Expected: `ad.json`, `norm.json`, `rn/model.json` + `.bin`.

- [ ] **Step 6: Commit**
```bash
git add src/satec/web_export tests/web_export web/public/models
git commit -m "feat(web): exportar Red Neuronal (TF.js) y Arbol (JSON)"
```

---

### Task 2: Precomputar riesgo por provincia + GeoJSON de la web

**Files:**
- Create: `src/satec/web_export/risk_map.py`
- Test: `tests/web_export/test_risk_map.py`

**Interfaces:**
- `latest_risk_table(df) -> pd.DataFrame` → por provincia, la fila de la **última semana**
  disponible (máx anio, máx semana) con `ubigeo_prov, casos, q3`.
- `build_risk_json(repo)` → entrena AD/RN sobre el histórico, predice el riesgo de la
  última semana por provincia y escribe `web/public/data/riesgo.json`
  (`{ubigeo_prov: {prob_rn, pred_ad, casos, nivel}}`) y `web/public/data/provincias.geojson`
  (solo provincias endémicas).

- [ ] **Step 1: Test de `latest_risk_table`** (`tests/web_export/test_risk_map.py`)

```python
import pandas as pd
from satec.web_export.risk_map import latest_risk_table


def test_latest_toma_ultima_semana_por_provincia():
    df = pd.DataFrame({
        "ubigeo_prov": ["0201", "0201", "0301"],
        "anio": [2024, 2024, 2024], "semana": [10, 48, 48],
        "casos": [1, 5, 2], "q3": [1.0, 1.0, 1.0]})
    out = latest_risk_table(df)
    fila = out[out["ubigeo_prov"] == "0201"]
    assert fila["semana"].iloc[0] == 48 and fila["casos"].iloc[0] == 5
```

- [ ] **Step 2: Ver fallo**
Run: `py -3.12 -m pytest "C:/Users/Usuario/Desktop/IA/SATEC-Carrion/tests/web_export/test_risk_map.py" -v`
Expected: FAIL `ModuleNotFoundError`

- [ ] **Step 3: Implementar `risk_map.py`**

```python
"""Precomputa el riesgo por provincia (ultima semana) para el mapa web."""
import json
import os
import pandas as pd

from satec.models.features_matrix import feature_matrix
from satec.models.train import train_decision_tree, train_neural_net, nn_predict_proba


def latest_risk_table(df: pd.DataFrame) -> pd.DataFrame:
    idx = (df.sort_values(["anio", "semana"])
             .groupby("ubigeo_prov").tail(1).index)
    return df.loc[idx].reset_index(drop=True)


def _nivel(prob: float) -> str:
    if prob >= 0.66:
        return "alto"
    if prob >= 0.33:
        return "medio"
    return "bajo"


def build_risk_json(repo: str = ".") -> None:
    df = pd.read_parquet(
        os.path.join(repo, "data/processed/dataset_enriched.parquet"))
    X, y = feature_matrix(df)
    ad = train_decision_tree(X, y, max_depth=8)
    model, norm = train_neural_net(X, y, epochs=60)

    latest = latest_risk_table(df)
    Xl, _ = feature_matrix(latest)
    prob_rn = nn_predict_proba(model, Xl, norm)
    pred_ad = ad.predict(Xl)

    riesgo = {}
    for i, row in latest.iterrows():
        p = float(prob_rn[i])
        riesgo[row["ubigeo_prov"]] = {
            "prob_rn": round(p, 3), "pred_ad": int(pred_ad[i]),
            "casos": int(row["casos"]), "nivel": _nivel(p)}

    out_dir = os.path.join(repo, "web", "public", "data")
    os.makedirs(out_dir, exist_ok=True)
    with open(os.path.join(out_dir, "riesgo.json"), "w", encoding="utf-8") as f:
        json.dump(riesgo, f, ensure_ascii=False)

    # GeoJSON solo de provincias endemicas presentes en el dataset
    geo_path = os.path.join(repo, "data/raw/peru_provincial.geojson")
    with open(geo_path, encoding="utf-8") as f:
        geo = json.load(f)
    endemicas = set(df["ubigeo_prov"].unique())
    geo["features"] = [ft for ft in geo["features"]
                       if str(ft["properties"]["FIRST_IDPR"]).zfill(4) in endemicas]
    with open(os.path.join(out_dir, "provincias.geojson"), "w",
              encoding="utf-8") as f:
        json.dump(geo, f, ensure_ascii=False)
    print(f"[OK] riesgo.json ({len(riesgo)} provincias) y provincias.geojson")


if __name__ == "__main__":
    build_risk_json()
```

- [ ] **Step 4: Tests + generación real**
Run:
```bash
REPO="C:/Users/Usuario/Desktop/IA/SATEC-Carrion"
py -3.12 -m pytest "$REPO/tests/web_export/test_risk_map.py" -v
PYTHONPATH="$REPO/src" TF_USE_LEGACY_KERAS=1 py -3.12 -c "from satec.web_export.risk_map import build_risk_json; build_risk_json(r'$REPO')"
```
Expected: `web/public/data/riesgo.json` (≈61 provincias) y `provincias.geojson`.

- [ ] **Step 5: Commit**
```bash
git add src/satec/web_export/risk_map.py tests/web_export/test_risk_map.py web/public/data
git commit -m "feat(web): precomputar riesgo por provincia y geojson endemico"
```

---

### Task 3: Interfaz web (mapa de riesgo + comparador)

**Files:**
- Create: `web/public/index.html`, `web/public/app.js`, `web/public/styles.css`
- Create: `web/vercel.json`, `web/.vercelignore`

**Nota:** esta tarea construye la UI. Usar un skill de diseño frontend (p. ej.
`frontend-design`) para lograr un acabado profesional. Requisitos funcionales:
- Mapa Leaflet con `provincias.geojson`, cada provincia coloreada por `nivel`
  (bajo/medio/alto) leído de `riesgo.json`.
- Popup/panel al hacer clic: nombre de provincia, prob. de la RN, predicción del AD, casos.
- Leyenda de niveles, título, y nota metodológica (datos MINSA + clima; modelo de apoyo).
- Tema visual propio (paleta de salud pública), responsive.

- [ ] **Step 1:** Construir `index.html` + `styles.css` + `app.js` (carga Leaflet por CDN,
  los dos JSON, pinta el mapa y maneja el clic). Verificar contra los archivos de `web/public/data`.
- [ ] **Step 2:** `vercel.json` (`outputDirectory: public`) y `.vercelignore`.
- [ ] **Step 3:** Servir localmente y verificar:
  `py -3.12 -m http.server --directory web/public 8000` → abrir `http://localhost:8000`.
- [ ] **Step 4: Commit** `git add web && git commit -m "feat(web): mapa de riesgo interactivo y comparador RN/AD"`

---

### Task 4: README de la web y verificación final

- [ ] Documentar en `web/README.md` cómo regenerar datos/modelos y desplegar.
- [ ] Verificar la suite completa del proyecto: `py -3.12 -m pytest`.
- [ ] Commit.

## Notas para el ejecutor

- La RN se exporta con `TF_USE_LEGACY_KERAS=1` (Keras 2), compatible con TF.js 4.22 (como el proyecto previo).
- `web/public/models` y `web/public/data` se versionan (la web los necesita).
- La inferencia en vivo (ajustar variables y predecir con TF.js) puede añadirse luego; el MVP usa riesgo precomputado por provincia.

## Próximo plan

**Plan 5 — Artículo ACM** con los resultados y las capturas de la web.
